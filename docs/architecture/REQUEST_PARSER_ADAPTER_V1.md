# Request Parser Adapter v1

Existing PDF/Excel parser output -> multilingual alias normalization -> RequestSnapshot -> persisted compare -> mismatch Review Task -> `/review`.

Principle: do not rewrite working parsers unnecessarily.
